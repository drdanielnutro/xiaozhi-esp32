# Fase FN — <nome da fase>

> Copie este arquivo para `fase-N.md` como **primeiro ato da fase** (quebra
> em tasks). Regra de ouro: cada checkbox é marcado **no mesmo commit** que
> conclui a task — o estado nunca pode divergir do código. No encerramento
> da fase, atualizar a tabela "Status das fases" do `plano-firmware.md` no
> commit final, após a revisão independente do Codex.

## Objetivo

<resumo da seção correspondente do plano-firmware.md>

## Pronto quando

<critério objetivo copiado do plano>

## Pendências físicas (hardware)

- <o que só pode ser validado na placa; escreva "nenhuma" se não houver>

## Tasks

- [ ] T1 — <descrição curta> · pronto quando: <critério verificável>
- [ ] T2 — <descrição curta> · pronto quando: <critério verificável>

## Notas da fase

<decisões do proxy relevantes (data + resumo), desvios do plano, commits>
